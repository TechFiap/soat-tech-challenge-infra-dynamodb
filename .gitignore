# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data based configuration files.
# *.tfvars
# *.tfvars.json

# Ignore override files as they are intended to be local-only and not part of version control.
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include plugin cache directories
.terraform.lock.hcl

# Include plan files as artifacts
plan.tfplan
tfplan
